@model MVCDemo.Models.Student
@{
    ViewBag.Title = "Index";
}

<h2>@(ViewBag.Message)</h2>
@if (Model == null)
{
    <h3>Model is null !!</h3>
}
else
{
    <h3>Name : @Model.Name</h3>
    <h4>Age : @Model.Age</h4>
}


